FROM nousresearch/hermes-agent:latest@sha256:46570f664fc62898acac9ca758fc8d4aa98e30964345163b875ed6600e3054b7

# Base image runs as root for s6-overlay bootstrap; keep the same runtime user.
# hadolint ignore=DL3002
USER root

COPY vendor/codex/install.sh /tmp/codex-install.sh

RUN sh /tmp/codex-install.sh \
    && rm -f /tmp/codex-install.sh \
    && cp /root/.codex/packages/standalone/current/bin/codex /usr/local/bin/codex \
    && chmod 0755 /usr/local/bin/codex \
    && /usr/local/bin/codex --version
